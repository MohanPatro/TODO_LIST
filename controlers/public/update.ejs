<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>

body{
    justify-content: center;
    align-items: center;
  background: linear-gradient(to bottom, #1c2e4a 0%, #23395d 50%, #2c446e 100%);
}

.container{
    
  background-color: transparent;
    border-radius: 10px;
    max-width: 450px;

    
}
#newtask{
    padding: 30px 20px;
}
#newtask input{
    width: 75%;
    height:30px;
    padding: 12px;
    color: #111111;
    font-weight: 500;
    border-radius: 5px;
    font-family: 'Poppins',sans-serif;
    font-size: 15px;
    border: 2px solid #d1d3d4;
    margin-bottom: 10px;
}

#newtask input:focus{
    outline: none;
    border-color: #0d75ec;
}
#newtask button{
 
    
    font-weight: 500;
    font-size: 16px;
    background-color: #0d75ec;
    border: none;
    color: #ffffff;
    cursor: pointer;
    outline: none;
    width: 20%;
    height: 45px;
    border-radius: 5px;
    font-family: 'Poppins',sans-serif;
}
    </style>
</head>
<body>
    <div class="container">
        <div id="newtask">
        <form action="/api/v1/tasks/update/<%- result[0].id %>" method='POST'>
            <input type="text" placeholder="Add Tasks" id="in" value="<%- result[0].name %>" name="name">
            <button id="push" type="submit">Update</button>
        </form>
           

        </div>
</body>
</html>